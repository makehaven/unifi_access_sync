unifi_access_sync.settings:
  type: config_object
  label: 'UniFi Access Sync settings'
  mapping:
    api_host:
      type: string
      label: 'UniFi Access API host'
    api_token:
      type: string
      label: 'UniFi Access API token'
    verify_ssl:
      type: boolean
      label: 'Verify SSL certificates'
    door_term_id:
      type: integer
      label: 'Door taxonomy term ID'
